node_modules/
.env
*.log

# Live SQLite — never commit
data/*.sqlite
data/*.sqlite-shm
data/*.sqlite-wal
data/*.db
data/*.db-shm
data/*.db-wal

# Local backup folders excluded from code push
backups/
db-backups/safety/

# Intentional DB snapshots in db-backups/*.sqlite are tracked via "Backup Database to GitHub" only
